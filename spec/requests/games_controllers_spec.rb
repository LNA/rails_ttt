require 'spec_helper'

RSpec.describe "GamesControllers", :type => :request do
  describe "POST /games_controllers" do
    it "works! (now write some real specs)" do
      # post 'new_game'
      # (:game).should_receive(:new)
      # (:game_presenter).should_receive(:new).with(:type_of_game)
      # response.should be okay # will tell if view is broken/doesn't existx
    end
  end
end
